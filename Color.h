#pragma once
#include <iostream>
#include <queue>
using namespace std;

enum Color { Red, Black }; // An enum is used here because we expect all nodes on a tree to be either Red or Black, and nothing else.